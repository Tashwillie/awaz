/** @type {import('next').NextConfig} */
const nextConfig = {
  // Removed env block - use .env.local for NEXT_PUBLIC_API_URL instead
  // Removed outputFileTracingRoot - not needed for standalone deployment
}

module.exports = nextConfig
